apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data-at-spark-int

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: ckan-config.yaml

labels:
  - pairs:
      app.kubernetes.io/environment: int
